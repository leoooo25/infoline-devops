# NodePort pour accéder à l'API depuis en dehors du cluster (Minikube local, pas de LoadBalancer réel)
apiVersion: v1
kind: Service
metadata:
  name: infoline-api
spec:
  type: NodePort
  selector:
    app: infoline-api
  ports:
    - port: 80
      targetPort: 8080
